# Core data
numpy
pandas>=2.0
openpyxl           # xlsx support for pd.read_excel
xlrd>=2.0.1        # xls support for pd.read_excel (IPP 2007–2018 files)
requests           # HTTP downloads
tqdm               # progress bars
scipy              # statistical distributions (wage_pension_distribution)

# Agent integration
mcp>=1.9           # optional MCP server wrappers around stattool data access

# Geospatial (Europe choropleth maps)
geopandas>=0.14
shapely            # geometry operations used by map rendering helpers

# Visualization
matplotlib>=3.8
matplotlib-scalebar  # optional: scale bar on maps

# Optional: direct Eurostat REST API client
# eurostat          # pip install eurostat  (wraps the SDMX/JSON API)
