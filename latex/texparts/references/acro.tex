% ==============================================================================
% Acronym Definitions
% ==============================================================================
% All \DeclareAcronym entries for the thesis.
% Package setup (\acsetup, templates, sorting helpers) and Czech translations
% are in CTUthesis.cls.  Only the thesis-specific declarations live here.
% ==============================================================================

% Czech locale strings for \printacronyms output.
% "s\abbrdot" produces "s." (strana).
\AddAcroTranslations{Czech}{%
  list-name  = Seznam pou\v{z}it\'ych zkratek,%
  page       = s\abbrdot,%
  pages      = s\abbrdot,%
  sequens    = a~d\'ale,%
  sequentes  = a~d\'ale,%
  also       = tak\'e,%
  and        = a,%
  or         = nebo%
}
\DeclareAcronym{OS}{
	short 	= OS,
	long 	= obráběcí stroj,
	tag		= zkr
	}
\DeclareAcronym{ANN}{
	short		= ANN,
	long 		= umělá neuronová síť,
	long-plural-form = umělé neuronové sítě,
	foreign 	= Artificial Neural Network,
	single = false,
	single-style = long-short,
	tag			= zkr
	}
\DeclareAcronym{FEM}{
	short		= FEM,
	alt			= MKP,
	long 		= metoda konečných prvků,
	foreign 	= Finite Element Method,
	single = false,
	single-style = long-short,
	tag			= zkr
	}
\DeclareAcronym{FFT}{
	short		= FFT,
	long 		= rychlá Fourierova transformace,
	foreign 	= Fast Fourier Transform,
	single = false,
	single-style = long-short,
	tag			= zkr
	}
\DeclareAcronym{FS~ČVUT}{
	short		= FS~ČVUT,
	first-style = short,
	long 		= {České vysoké učení technické v~Praze, Fakulta strojní},
	tag			= zkr
	}
\DeclareAcronym{MA}{
	short		= MA,
	long 		= klouzavý průměr,
	foreign 	= Moving Average,
	single = false,
	single-style = long-short,
	tag			= zkr
	}
\DeclareAcronym{ML}{
	short		= ML,
	long 		= strojové učení,
	foreign 	= Machine Learning,
	tag			= zkr
	}
\DeclareAcronym{NC}{
	short		= NC,
	long 		= číslicové řízení,
	long-plural-form = číslicovým řízením, %instrumental
	foreign 	= Numerical Control,
	tag			= zkr
	}